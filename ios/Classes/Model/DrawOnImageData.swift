struct DrawOnImageData {
    let stringToWrite: String
    let leftPadding: Int
    let rightPadding: Int
    let topPadding: Int
    let bottomPadding: Int
    let fontColor: Int
    let fontSize: Int
    let imageCanvas: Data
}
